<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <title>Show Post</title>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbo-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbo-track': 'reload' %>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 font-sans leading-normal tracking-normal">
  <div class="container text-white mx-auto p-4">
    <h1 class="text-4xl font-bold mb-4"><%= @post.author %></h1>
    <p class="text-gray-400 mb-2">Location: <%= @post.location %></p>
    <p class="text-gray-400 mb-2">Rent: <%= @post.rent %></p>
    <p class="text-gray-400 mb-2">Preferred Gender: <%= @post.preferred_gender %></p>

    <div class="flex space-x-4">
      <%= link_to 'Edit', edit_post_path(@post), class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" %>
      <%= link_to 'Back', posts_path, class: "bg-gray-500 hover:bg-gray-700 text-white font-bold py-2 px-4 rounded" %>
      <%= link_to 'Delete', @post, method: :delete, data: { confirm: 'Are you sure?' }, class: "bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded" %>
    </div>
  </div>
</body>
</html>
